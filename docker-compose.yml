version: "3.8"

services:
  aptd:
    image: ${REGISTRY:-docker.io}/penikolov23/adastra-python-task-dockerized:${BUILD_TAG:-latest}

  postgres-db:
    image: postgres
    env_file:
      - database.env
    ports:
      - target: 5432
        published: 5432
        protocol: tcp
        mode: host
    healthcheck:
      test: pg_isready -U postgres
      interval: 10s
      timeout: 5s 
      retries: 5
